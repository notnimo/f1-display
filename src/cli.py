def isTuiNecessary(args: list) -> list:
  """
  Args:
  	args: a list of the args that the program was launched on
    
  Returns:
  	a list where the first element is a boolean that determinates if the tui selector is necessary, if the flag is true then the rest of the list are the identifiers for the needed fields	"""
  return [True]